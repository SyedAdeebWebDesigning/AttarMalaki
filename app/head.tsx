export default function Head() {
	return (
		<>
			<link
				rel="preload"
				href="/WhyChooseUs.webp"
				as="image"
				type="image/webp"
				// Optional: Add sizes attribute if image used responsively
				sizes="100vw"
			/>
			{/* Add other head tags like meta, title, etc. */}
		</>
	);
}
